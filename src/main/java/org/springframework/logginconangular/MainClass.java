/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package org.springframework.logginconangular;

import java.util.ArrayList;

/**
 *
 * @author root
 */
public class MainClass {
    /*
    public static void main(String[] args) throws Exception {
        String n="alberto";
        String m ="12345";
        LogginUsuario ls = new LogginUsuario();
                
        Usuario u = new Usuario();
        Usuario u2 = new Usuario();
        DAOUsuario dao = new DAOUsuario();
        u.setEmail(n);
        u.setPassword(m);
        try{
            u2 = ls.buscarUsuario(u);
        System.out.println("El email es " +u2.getEmail() + "el ide es -> " + u2.getIdUsuario() + "El nombre es ->" + u2.getNombre());
        }catch(Exception e){
            System.out.println("El usuario no existe");
        }
        
        /*
        ArrayList<Usuario> usuarios =dao.obtenerUsuario(u);
        if(usuarios.size()> 0){
            for(Usuario usuariop : usuarios){
                System.out.println(usuariop.getNombre() + "->" + "->" +usuariop.getEmail() + "->" + usuariop.getPassword() );
            }
        }else{
            System.out.println("El usuario no existe");
        }
        
    }
*/
}
